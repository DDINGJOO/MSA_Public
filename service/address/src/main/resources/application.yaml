server:
  port: 9201

spring:

  datasource:
    url: jdbc:mysql://host.docker.internal:3306/address
    username: root
    password: 1001
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update  # 개발 단계에만 사용 (운영에서는 validate 또는 none)
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

KAKAO_KEY : f729f3116faeb0d8dafb637d913d9d33
